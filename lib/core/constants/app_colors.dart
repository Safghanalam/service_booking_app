import 'package:flutter/material.dart';

class AppColors {
  // Primary and Secondary Colors
  static const Color primary = Color.fromRGBO(33, 41, 50, 1);
  static const Color secondary = Color.fromRGBO(228, 231, 235, 1);
  static const Color backgroundColor = Colors.white;
  static const Color textColor = Color.fromRGBO(53, 63, 64, 1);

  // Button Colors
  static const Color buttonSecondaryColor = Color.fromRGBO(189, 189, 189, 1);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);
  static const Color error = Color(0xFFF44336);
}
